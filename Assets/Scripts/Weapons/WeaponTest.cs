using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponTest : MonoBehaviour
{
    private Pistol pistol;
    private WeaponClass selectedWeapon;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        /*
        if (Input.GetKeyDown(KeyCode.U))
        {
            pistol = new Pistol(1);
            Debug.Log(pistol.gunName + " " + pistol.damage + " " + pistol.bulletSpeed + " " + pistol.fireRate);
        }

        if (Input.GetKeyDown(KeyCode.I))
        {
            pistol = new Pistol(2);
            Debug.Log(pistol.gunName + " " + pistol.damage + " " + pistol.bulletSpeed + " " + pistol.fireRate);
        }

        if (Input.GetKeyDown(KeyCode.O))
        {
            pistol = new Pistol(3);
            Debug.Log(pistol.gunName + " " + pistol.damage + " " + pistol.bulletSpeed + " " + pistol.fireRate);
        }
        if (Input.GetKeyDown(KeyCode.P))
        {
            pistol = new Pistol(4);
            Debug.Log(pistol.gunName + " " + pistol.damage + " " + pistol.bulletSpeed + " " + pistol.fireRate);
        }

        if (Input.GetKeyDown(KeyCode.L))
        {
            selectedWeapon = pistol;
            Debug.Log(selectedWeapon.gunName + " " + selectedWeapon.damage + " " + selectedWeapon.bulletSpeed + " " + selectedWeapon.fireRate);
        }

        */
    }
       

}
